#include<bits/stdc++.h>
using namespace std;

int main(){
   // int a,b;
   // cout<<"enter the numbers";
    //cin>> a >>b ;
    //cout<<"the value of a is "<< a <<"  , the value of b is"<< b;

    string s;
    cin>>s;
    cout<<s;
// getline function is used to take whole line as a string input
    string str;
    getline(cin,str);
    cout<<str;
    return 0;
}